@{
    var db = Database.Open("Go-Go-Go--Knight-s-Story");
    var resultQuery = db.Query(@"SELECT * FROM Map");

    <table>
        <tr>
            <th>編號</th>
            <th>名稱</th>
            <th>等級限制</th>
        </tr>

        @foreach(var result in resultQuery){
            <tr>
                <td>@result.Map_Id</td>
                <td>@result.Map_Name</td>
                <td>@result.Level_Limit</td>
            </tr>
        }
    </table>  
}
